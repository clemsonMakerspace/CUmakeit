These files are used to manage the database properly. 
Copy and paste all of the test into a MySQL workbench query window to execute. 
'Create' will make all of the tables. 
'Majors' populates the major tables; 
'Drop' can be used to reset the database by deleting every table; every table can then be recreated with the other two files. 